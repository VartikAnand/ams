import React from "react";

interface CreateSalaryProps {
  initialData: string;
  salId: string;
  empId: string;
}
export const CreateSalary = ({
  initialData,
  empId,
  salId,
}: CreateSalaryProps) => {
  return <div>CreateSalary</div>;
};
